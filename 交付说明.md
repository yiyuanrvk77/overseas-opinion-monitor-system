# 社科院海外舆情监测系统 V1.0

## 交付范围

本交付包是可独立运行的本地产品，包含前端构建产物、后端服务、测试样本快照、启动脚本和产品技术文档。系统不依赖外部代码仓库或外部在线页面才能启动。

正式工作台保留 9 个业务页面：总览、监测对象、采集与批次、知识库、知识图谱、分析研判、预警中心、报告中心和智能问答。需求验收、质量与审计属于产品研发和运维治理内容，不作为正式产品导航页面；相关说明保留在技术文档、测试和内部维护接口中。

## 本地启动

1. Windows 用户双击 `start.bat`，或在 PowerShell 中运行 `start.ps1`。
2. 浏览器打开 `http://127.0.0.1:8000`。
3. 首次运行会创建本地 Python 环境并安装 `backend/requirements.txt` 中的依赖；前端静态构建产物已随包提供。
4. 服务启动时会保留 `src/zipSnapshot.json` 回归批次。部署公开数据时先运行 `scripts/collect_public_web.py`，再运行 `scripts/import_public_demo.py`；导入器会把公开批次设为工作台活动批次。

## 数据边界

当前产品支持公开网页试采批次与 ZIP 回归批次。公开批次只保存直接可读的标题、短摘要、时间、来源链接和渠道访问证据；受登录、验证码、付费或访问控制限制的内容不会被绕过。ZIP 批次只用于回归验证。系统展示的事实、关系和指标均应回溯到批次记录、来源链接、证据片段或模型运行记录。

## 包内文件

- `backend/`：FastAPI 服务、SQLite 数据模型、数据导入、接口和后端测试。
- `src/`：React 前端源码、回归样本快照和页面样式。
- `data/`、`scripts/`：公开网页快照、12 渠道采集器和幂等导入工具。
- `dist/`：已构建的前端静态文件，便于离线启动。
- `docs/`：产品技术文档 V1.0。
- `start.ps1`、`start.bat`：本地启动入口。
- `README.md`：项目使用说明。

## 已完成验证

- 后端单元和接口测试：50 项全部通过。
- 前端生产构建：通过。
- 文档结构与可访问性检查：通过，无高、中、低级问题。
- 已验证桌面和窄屏页面无横向溢出，正式导航不包含内部治理页面。

## 生产化边界

当前交付定位为可单机部署产品和可替换公开数据批次。对公网开放时必须启用服务端认证或带认证的 HTTPS 反向代理；供应商接口签约后仍需补齐正式连接器、模型评测集、密钥管理、恢复演练、监控告警和多实例数据层，详见部署与技术文档。
